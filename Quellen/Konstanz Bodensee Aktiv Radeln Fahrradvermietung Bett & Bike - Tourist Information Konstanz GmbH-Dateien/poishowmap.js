function showslidemap(id,lng,lat,title){
	//makeMap();
	
		//alert(parent.frames[0].name);
	
	//alert(parent.mapframe.document.forms[0])
	
	location.href="#inhalt";
	
	/*
	var test = parent.mapframe.//funktion, die ein GLatLng-Objekt erzeugt
	var test = parent.mapframe.GLatLng(3,9.172061,47.661279);
	marker = parent.mapframe.createMarker(new GLatLng(lat,lng), id, 'standardpoi.png', title,'tt_address');
	parent.mapframe.map.addOverlay( marker );
	parent.mapframe.myclick(id,lng,lat,'tt_address');
	*/
	
	//$('mapslider').setStyle('height', '450px');
}
/*
window.addEvent('domready', function() {
	var scroll = new Fx.Scroll(Window, {
    	wait: false,
    	duration: 500,
    	offset: {'x': 0, 'y': 0},
    	transition: Fx.Transitions.Quad.easeInOut
	});
	if($('poishowmap')){
		$('poishowmap').addEvent('click', function(event) {
			event = new Event(event).stop();
			scroll.toElement('top');
		});
	}
});*/
